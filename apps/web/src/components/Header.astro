---
import { withBase } from "../lib/url";

interface Props {
  current?: "home" | "docs";
}

const { current } = Astro.props;
---

<header class="site-header">
  <div class="nav">
    <a class="brand" href={withBase("/")}>
      <span class="tick">+</span>
      eurocloud
      <span class="tick">+</span>
    </a>
    <nav class="nav-links" aria-label="Primary">
      <a href={withBase("/docs/install")} aria-current={current === "docs" ? "page" : undefined}>docs</a>
      <a href="https://github.com/valtterisa/eurocloud" rel="noreferrer">github</a>
    </nav>
  </div>
</header>
